const config = {
  plugins: {
    "@tailwindcss/postcss": {
      theme: {
        colors: {
          // Your custom colors here
          primary: "#ff1e00",
          secondary: "#1F1F1F",

          // Custom color with variants

          // Named colors
        },
      },
    },
  },
};

export default config;
